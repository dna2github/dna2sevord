# rust package mirror

```ini
[source.crates-io]
# uncomment one line below to use one mirror to download packages
# for `cargo install`
#replace-with = "aliyun"
#replace-with = "ustc"

[source.aliyun]
registry = "https://code.aliyun.com/rustcc/crates.io-index"

[source.ustc]
registry = "https://mirrors.ustc.edu.cn/crates.io-index"
```
